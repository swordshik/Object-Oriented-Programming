from typing import List

class Solution:
    def arraySign(self, nums: List[int]) -> int:
        n = 1
        for i in range(len(nums)):
            n = n*nums[i]
        if n>0:
            return 1
        elif n < 0:
            return -1
        else:
            return 0